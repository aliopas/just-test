# اختبار Story 3.4: رفع الملفات - دليل سريع

**التاريخ:** 2025-01-17  
**الحالة:** 🧪 جاهز للاختبار

---

## 🚀 البدء السريع

### 1. تشغيل الاختبارات الآلية (Backend)

```bash
# من المجلد الرئيسي
npm test -- request.controller.test.ts --testNamePattern="presignAttachment"
```

أو تشغيل جميع اختبارات الطلبات:

```bash
npm test -- request.controller.test.ts
```

### 2. الاختبار اليدوي (Frontend)

#### الخطوات:
1. **تشغيل Backend:**
   ```bash
   npm run dev
   ```

2. **تشغيل Frontend:**
   ```bash
   cd frontend
   npm run dev
   ```

3. **فتح المتصفح:**
   - انتقل إلى `http://localhost:3000` (أو المنفذ المحدد)
   - سجّل الدخول بحساب مستخدم

4. **اختبار رفع الملفات:**
   - اذهب إلى صفحة "إنشاء طلب جديد"
   - اتبع السيناريوهات في `STORY_3.4_MANUAL_TEST_CHECKLIST.md`

---

## ✅ قائمة التحقق السريعة

### الاختبارات الأساسية (5 دقائق)
- [ ] رفع ملف PDF بعد إنشاء الطلب
- [ ] رفع ملف JPG بعد إنشاء الطلب
- [ ] رفع عدة ملفات في نفس الوقت
- [ ] Drag & Drop للملفات

### اختبارات الأخطاء (3 دقائق)
- [ ] ملف بحجم > 10MB (يجب أن يفشل)
- [ ] ملف بنوع غير مدعوم (يجب أن يفشل)
- [ ] رفع ملف قبل إنشاء الطلب (يجب أن يكون معطلاً)

### اختبارات UX (2 دقائق)
- [ ] عرض حالة الرفع (pending, uploading, success, error)
- [ ] RTL/LTR support
- [ ] رسائل الخطأ واضحة

**الوقت الإجمالي:** ~10 دقائق

---

## 🔍 التحقق من النجاح

### علامات النجاح:
- ✅ الملفات تُرفع بنجاح
- ✅ حالة الرفع تظهر بشكل صحيح
- ✅ رسائل الخطأ واضحة
- ✅ الملفات تظهر في Request Details
- ✅ السجلات تُنشأ في Database

### علامات الفشل:
- ❌ الملفات لا تُرفع
- ❌ رسائل خطأ غير واضحة
- ❌ حالة الرفع لا تتحدث
- ❌ الملفات لا تظهر في Database

---

## 📝 سجل الاختبار

**المختبر:** _________________  
**التاريخ:** _________________  
**الوقت:** _________________

### النتائج:

| الاختبار | الحالة | ملاحظات |
|----------|--------|---------|
| رفع PDF | ⬜ | |
| رفع JPG | ⬜ | |
| رفع عدة ملفات | ⬜ | |
| Drag & Drop | ⬜ | |
| ملف > 10MB | ⬜ | |
| نوع غير مدعوم | ⬜ | |
| رفع قبل إنشاء الطلب | ⬜ | |
| حالة الرفع | ⬜ | |
| RTL/LTR | ⬜ | |

### المشاكل المكتشفة:
```
_________________________________________________
_________________________________________________
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: الملفات لا تُرفع
**الحل:**
1. تحقق من `requestId` موجود في Console
2. تحقق من Presigned URL في Network tab
3. تحقق من CORS settings في Supabase

### المشكلة: رسائل الخطأ غير واضحة
**الحل:**
1. تحقق من Console للأخطاء
2. تحقق من Network tab للـ API responses
3. تحقق من Error handling في `UploadDropzone`

### المشكلة: الملفات لا تظهر في Database
**الحل:**
1. تحقق من Supabase Dashboard
2. تحقق من جدول `attachments`
3. تحقق من Storage bucket `request-attachments`

---

## ✅ النتيجة النهائية

**الحالة:** ⬜ نجاح / ⬜ فشل

**التوصيات:**
```
_________________________________________________
_________________________________________________
```

---

**ملاحظة:** للحصول على دليل تفصيلي، راجع:
- `STORY_3.4_MANUAL_TEST_CHECKLIST.md` - قائمة التحقق الكاملة
- `docs/stories/STORY_3.4_TESTING_GUIDE.md` - دليل الاختبار الشامل

