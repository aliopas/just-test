# دليل اختبار إرسال الطلبات
# Testing Guide for Request Forms Submission

## 🚀 البدء السريع
## Quick Start

### 1. تشغيل المشروع محليًا
### 1. Running the Project Locally

#### Backend:
```bash
# من المجلد الرئيسي للمشروع
npm run dev

# أو من داخل مجلد backend:
cd backend
npm run dev
```

Backend سيعمل على: `http://localhost:3001` (أو PORT محدد في `.env`)

#### Frontend:
```bash
# من مجلد frontend:
cd frontend
npm run dev
```

Frontend سيعمل على: `http://localhost:5173` (Vite default port)

> **ملاحظة:** تأكد من تشغيل Backend قبل Frontend لضمان اتصال API بشكل صحيح.

### 2. تسجيل الدخول
### 2. Login

استخدم الحساب التجريبي:
- **Email:** `investor.demo@invastors.dev`
- **Password:** `InvestorDemo123!`

أو سجل حساب مستثمر جديد.

### 3. الوصول إلى صفحة إنشاء الطلب
### 3. Access the New Request Page

اذهب إلى: `http://localhost:5173/requests/new`

سترى `RequestTypeSelector` مع جميع الخيارات:
- شراء / Buy
- بيع / Sell
- شراكة / Partnership
- ترشيح مجلس / Board Nomination
- ملاحظات / Feedback

---

## 🧪 خطوات الاختبار
## Testing Steps

### 1. اختبار PartnershipRequestForm
### Testing PartnershipRequestForm

#### الخطوات:
#### Steps:

1. افتح الصفحة: `/requests/new`
2. اختر نوع الطلب: **"شراكة" / "Partnership"**
3. املأ الحقول المطلوبة:
   - **اسم الشركة** (Company Name): `شركة تجريبية`
   - **نوع الشراكة** (Partnership Type): `استراتيجية` / `Strategic`
   - **اسم الشخص المسؤول** (Contact Person): `أحمد محمد`
   - **البريد الإلكتروني** (Email): `test@example.com`
   - **مبلغ الاستثمار** (Investment Amount): `100000` (اختياري)
   - **تفاصيل الشراكة** (Partnership Details): `تفاصيل تجريبية`
   - **رقم الهاتف** (Phone): `+966501234567` (اختياري)

4. اضغط على **"إرسال الطلب" / "Submit Request"**

#### ✅ النتيجة المتوقعة:
#### ✅ Expected Result:

- ✅ يتم إنشاء الطلب بنجاح
- ✅ تظهر رسالة نجاح: "تم إنشاء طلب الشراكة بنجاح"
- ✅ يمكن رفع المرفقات (اختياري)
- ✅ الطلب يظهر في قاعدة البيانات بنوع `partnership`

#### 🔍 التحقق من البيانات المرسلة:
#### 🔍 Verify Sent Data:

- `type`: `"partnership"`
- `metadata.companyName`: `"شركة تجريبية"`
- `metadata.partnershipType`: `"strategic"`
- `metadata.contactPerson`: `"أحمد محمد"`
- `metadata.contactEmail`: `"test@example.com"`
- `amount`: `100000` (إذا تم توفيره)
- `currency`: `"SAR"` (إذا تم توفير amount)

---

### 2. اختبار BoardNominationRequestForm
### Testing BoardNominationRequestForm

#### الخطوات:
#### Steps:

1. افتح الصفحة: `/requests/new`
2. اختر نوع الطلب: **"ترشيح مجلس" / "Board Nomination"**
3. املأ الحقول المطلوبة:
   - **اسم المرشح** (Nominee Name): `محمد علي`
   - **المنصب المقترح** (Proposed Position): `عضو مجلس إدارة`
   - **المؤهلات والخبرات** (Qualifications): `خبرة 10 سنوات في القطاع المالي`
   - **سبب الترشيح** (Nomination Reason): `سبب الترشيح هنا`
   - **البريد الإلكتروني** (Email): `nominee@example.com` (اختياري)
   - **رقم الهاتف** (Phone): `+966501234567` (اختياري)

4. اضغط على **"إرسال الطلب" / "Submit Request"**

#### ✅ النتيجة المتوقعة:
#### ✅ Expected Result:

- ✅ يتم إنشاء الطلب بنجاح
- ✅ تظهر رسالة نجاح: "تم إنشاء طلب الترشيح بنجاح"
- ✅ يمكن رفع المرفقات (اختياري)
- ✅ الطلب يظهر في قاعدة البيانات بنوع `board_nomination`
- ✅ **لا يتم إرسال `amount` أو `currency`**

#### 🔍 التحقق من البيانات المرسلة:
#### 🔍 Verify Sent Data:

- `type`: `"board_nomination"`
- `metadata.nomineeName`: `"محمد علي"`
- `metadata.nomineePosition`: `"عضو مجلس إدارة"`
- `metadata.nomineeQualifications`: `"خبرة 10 سنوات في القطاع المالي"`
- `metadata.nominationReason`: `"سبب الترشيح هنا"`
- **لا يوجد `amount` أو `currency` في الطلب**

---

### 3. اختبار FeedbackRequestForm
### Testing FeedbackRequestForm

#### الخطوات:
#### Steps:

1. افتح الصفحة: `/requests/new`
2. اختر نوع الطلب: **"ملاحظات" / "Feedback"**
3. املأ الحقول المطلوبة:
   - **نوع الملاحظة** (Feedback Type): `اقتراح` / `Suggestion`
   - **الأولوية** (Priority): `متوسطة` / `Medium`
   - **الموضوع** (Subject): `اقتراح تحسين`
   - **الرسالة** (Message): `أقترح إضافة ميزة جديدة...`

4. اضغط على **"إرسال الملاحظات" / "Submit Feedback"**

#### ✅ النتيجة المتوقعة:
#### ✅ Expected Result:

- ✅ يتم إنشاء الطلب بنجاح
- ✅ تظهر رسالة نجاح: "تم إرسال الملاحظات بنجاح"
- ✅ يمكن رفع المرفقات (اختياري)
- ✅ الطلب يظهر في قاعدة البيانات بنوع `feedback`
- ✅ **لا يتم إرسال `amount` أو `currency`**

#### 🔍 التحقق من البيانات المرسلة:
#### 🔍 Verify Sent Data:

- `type`: `"feedback"`
- `metadata.feedbackType`: `"suggestion"`
- `metadata.priority`: `"medium"`
- `metadata.subject`: `"اقتراح تحسين"`
- `notes`: `"أقترح إضافة ميزة جديدة..."`
- **لا يوجد `amount` أو `currency` في الطلب**

---

## 🔍 التحقق من قاعدة البيانات
## Database Verification

### بعد إرسال كل طلب، تحقق من:

1. **في Supabase:**
   ```sql
   SELECT 
     id,
     request_number,
     type,
     amount,
     currency,
     metadata,
     status,
     created_at
   FROM requests
   WHERE type IN ('partnership', 'board_nomination', 'feedback')
   ORDER BY created_at DESC
   LIMIT 10;
   ```

2. **تحقق من:**
   - ✅ `type` صحيح
   - ✅ `metadata` يحتوي على البيانات المطلوبة
   - ✅ للأنواع غير المالية: `amount` و `currency` يجب أن تكون `NULL` (إلا إذا كان اختياريًا)

---

## 🐛 استكشاف الأخطاء
## Troubleshooting

### مشكلة: خطأ 500 عند الإرسال

**التحقق:**
1. افتح Developer Console (F12)
2. تحقق من Network Tab - انظر إلى Request Payload
3. تحقق من Response - ما هي رسالة الخطأ؟

**الحلول المحتملة:**
- تحقق من أن جميع الحقول المطلوبة مملوءة
- تحقق من صحة البيانات (مثل البريد الإلكتروني)
- تحقق من Backend logs

---

### مشكلة: لا يظهر النموذج الصحيح

**التحقق:**
1. تحقق من أن `RequestTypeSelector` يعرض جميع الخيارات
2. تحقق من أن `DynamicRequestForm` يعمل بشكل صحيح
3. تحقق من Browser Console للأخطاء

---

### مشكلة: يرسل `amount` للأنواع غير المالية

**التحقق:**
1. تحقق من `BoardNominationRequestForm` - يجب ألا يرسل `amount`
2. تحقق من `FeedbackRequestForm` - يجب ألا يرسل `amount`
3. تحقق من `PartnershipRequestForm` - يرسل `amount` فقط إذا تم توفيره

---

## ✅ قائمة التحقق
## Checklist

### قبل الاختبار:
- [ ] Frontend تم بناءه بشكل صحيح
- [ ] Backend يعمل بشكل صحيح
- [ ] قاعدة البيانات متصلة

### بعد كل اختبار:
- [ ] ✅ الطلب تم إنشاؤه بنجاح
- [ ] ✅ البيانات في قاعدة البيانات صحيحة
- [ ] ✅ النوع صحيح (`partnership`, `board_nomination`, `feedback`)
- [ ] ✅ `metadata` يحتوي على البيانات المطلوبة
- [ ] ✅ `amount` و `currency` صحيحين (NULL للأنواع غير المالية)

---

## 📝 ملاحظات
## Notes

- جميع الاختبارات يجب أن تتم بحساب مستثمر مسجل
- يمكن رفع المرفقات بعد إنشاء الطلب
- يمكن متابعة حالة الطلب من صفحة `/requests`

---

**آخر تحديث:** 2025-01-30
**Last Updated:** 2025-01-30

