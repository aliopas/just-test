# حل عاجل: متغيرات البيئة في Netlify
# URGENT: Netlify Environment Variables Fix

## ⚠️ المشكلة الحالية
## Current Problem

```
[Supabase Config Debug] {
  hasWindowEnv: true,
  windowEnvUrl: 'missing',
  windowEnvKey: 'missing',
  viteEnvUrl: 'missing',
  viteEnvKey: 'missing',
  allEnvKeys: []
}
```

**السبب:** المتغيرات غير موجودة في Netlify أو لم يتم إعادة النشر بعد إضافتها.

---

## ✅ الحل السريع (5 دقائق)
## Quick Fix (5 minutes)

### الخطوة 1: فتح Netlify Dashboard

1. اذهب إلى: https://app.netlify.com
2. سجّل الدخول
3. ابحث عن موقعك: **investor-bacura**
4. اضغط على الموقع

### الخطوة 2: فتح Environment Variables

1. من القائمة الجانبية: **Site settings**
2. اضغط على: **Environment variables**
   - أو: **Build & deploy** → **Environment**

### الخطوة 3: إضافة المتغير الأول

1. اضغط على **"Add a variable"** (إضافة متغير)

2. **Key (المفتاح):**
   ```
   VITE_SUPABASE_URL
   ```

3. **Value (القيمة):**
   ```
   https://wtvvzthfpusnqztltkkv.supabase.co
   ```

4. **Scope (النطاق):**
   - ✅ فعّل **Production** (مهم جداً!)
   - يمكنك تفعيل **Deploy previews** و **Branch deploys** أيضاً

5. اضغط على **"Save"** (حفظ)

### الخطوة 4: إضافة المتغير الثاني

1. اضغط على **"Add a variable"** مرة أخرى

2. **Key (المفتاح):**
   ```
   VITE_SUPABASE_ANON_KEY
   ```

3. **Value (القيمة):**
   ```
   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Ind0dnZ6dGhmcHVzbnF6dGx0a2t2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjIyMzE2MDUsImV4cCI6MjA3NzgwNzYwNX0.6KttJmjGUsgp3xfGf3wBm6kPmrinXB5R6AJJsTB-LWA
   ```

4. **Scope (النطاق):**
   - ✅ فعّل **Production** (مهم جداً!)

5. اضغط على **"Save"** (حفظ)

### الخطوة 5: إعادة النشر (مهم جداً!)

**⚠️ بدون هذه الخطوة، لن تعمل المتغيرات!**

1. من القائمة الجانبية: **Deploys**

2. اضغط على **"Trigger deploy"** (في الأعلى)

3. اختر **"Clear cache and deploy site"**
   - ⚠️ لا تختار "Deploy site" فقط
   - ⚠️ يجب اختيار "Clear cache and deploy site"

4. انتظر حتى يكتمل النشر:
   - قد يستغرق 2-5 دقائق
   - ستظهر رسالة "Published" عند النجاح

---

## ✅ التحقق من النجاح
## Verify Success

### بعد إكمال النشر:

1. **افتح الموقع:**
   - اذهب إلى: `https://investor-bacura.netlify.app`

2. **افتح Console (F12):**
   - اضغط F12 → Console

3. **ابحث عن:**
   - يجب أن ترى: `[Supabase Config Debug]` مع:
     - ✅ `viteEnvUrl: 'set'`
     - ✅ `viteEnvKey: 'set'`
     - ✅ `allEnvKeys: ['VITE_SUPABASE_URL', 'VITE_SUPABASE_ANON_KEY']`
   - يجب ألا ترى:
     - ❌ `[Supabase] Missing configuration`

4. **اختبر Password Reset:**
   - اذهب إلى صفحة Login
   - انقر على "نسيت كلمة المرور؟"
   - يجب أن يعمل بدون أخطاء

---

## ❌ إذا استمرت المشكلة
## If Problem Persists

### 1. تحقق من Build Logs

1. **Deploys** → اختر آخر نشر
2. اضغط على **"Build logs"**
3. ابحث عن:
   - أي أخطاء متعلقة بالمتغيرات
   - رسالة نجاح البناء

### 2. تحقق من Scope

لكل متغير:
1. اضغط على السهم ⬇️ بجانب المتغير
2. تأكد من أن **Production** مفعّل ✓
3. إذا لم يكن مفعّلاً، فعّله واحفظ

### 3. احذف وأعد إضافة المتغيرات

1. احذف `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY`
2. أعد إضافتها بالطريقة المذكورة أعلاه
3. تأكد من أن Scope هو **Production** ✓
4. أعد النشر باستخدام **"Clear cache and deploy site"**

---

## 📋 قائمة التحقق
## Checklist

- [ ] ✅ تم إضافة `VITE_SUPABASE_URL` في Netlify
- [ ] ✅ تم إضافة `VITE_SUPABASE_ANON_KEY` في Netlify
- [ ] ✅ Scope هو **Production** ✓ لكل متغير
- [ ] ✅ القيم صحيحة (بدون مسافات إضافية)
- [ ] ✅ تم إعادة النشر باستخدام **"Clear cache and deploy site"**
- [ ] ✅ النشر اكتمل بنجاح (Published)
- [ ] ✅ الموقع يعمل بدون أخطاء Supabase

---

## 🔑 ملاحظات مهمة
## Important Notes

1. **البادئة `VITE_` ضرورية:**
   - ✅ صحيح: `VITE_SUPABASE_URL`
   - ❌ خطأ: `SUPABASE_URL` (لن يعمل)

2. **Scope مهم جداً:**
   - يجب أن يكون **Production** مفعّل ✓
   - بدون ذلك، لن تكون المتغيرات متاحة أثناء البناء

3. **إعادة النشر ضرورية:**
   - Vite يدمج المتغيرات في وقت البناء
   - بعد إضافة المتغيرات، يجب إعادة البناء والنشر

4. **استخدم "Clear cache":**
   - يضمن مسح الكاش القديم
   - يضمن تطبيق التغييرات بشكل صحيح

---

## 🆘 الدعم
## Support

إذا استمرت المشكلة بعد تجربة جميع الخطوات:

1. راجع Build Logs في Netlify
2. راجع Console في المتصفح
3. تأكد من أن المتغيرات موجودة في Production scope
4. تأكد من إعادة النشر بعد أي تعديل

---

## 📞 روابط مباشرة
## Direct Links

- **Netlify Dashboard:** https://app.netlify.com
- **Environment Variables:** اذهب إلى موقعك → Site settings → Environment variables
- **Deploys:** اذهب إلى موقعك → Deploys

