# Story 5.1: إنشاء جداول المحتوى – حالة الإكمال

**التاريخ:** 2025-11-08  
**الحالة:** ✅ مكتمل

---

## ✅ ما تم إنجازه

### 1. بنية قاعدة البيانات للمحتوى
- إنشاء الهجرة `20241108092000_news_tables.sql` التي تقوم بالتالي:
  - تعريف نوع `news_status` (draft | scheduled | published).
  - إنشاء جدول `news_categories` مع الحقول المطلوبة (الاسم، الـ slug، الوصف، الطوابع الزمنية).
  - إنشاء جدول `news` مع جميع الحقول المطلوبة (العنوان، الـ slug، نص Markdown، صورة الغلاف، التصنيف، الحالة، مواعيد الجدولة والنشر، المرسل، الطوابع الزمنية).
  - ربط `news.category_id` بجدول `news_categories`، وربط `news.author_id` بجدول `users`.

### 2. الفهارس والبحث
- إنشاء مؤشرات على الحقول الرئيسية (`category_id`, `status`, `scheduled_at`, `published_at`) لتحسين الاستعلامات.
- إعداد مؤشر GIN لكامل النص باستخدام `to_tsvector('simple', title + body_md)` لتلبية متطلبات البحث السريع في Supabase.

### 3. سياسات RLS
- تفعيل RLS لكلٍ من `news` و `news_categories`.
- إضافة سياسات تسمح فقط لأصحاب صلاحية `admin.content.manage` (من خلال RBAC) بقراءة وإدارة الجداول، تمهيداً لقصص الـ API والواجهة في القصص اللاحقة.

---

## 🧪 الاختبارات
- لم يتم إضافة اختبارات جديدة في هذه القصة، وتم التأكد من مرور `npm run lint` و `npm run test` كجزء من تدفق العمل الاعتيادي.

---

## 📁 الملفات المتأثرة
- `supabase/migrations/20241108092000_news_tables.sql`
- المستند الحالي `docs/stories/STORY_5.1_COMPLETION.md`

---

## 📌 ملاحظات
- سيتم استخدام الحقول الجديدة في قصص Epic 5 اللاحقة (CRUD، الواجهة، الجدولة…).
- سيتم تحديث سياسات RLS لاحقاً للسماح بالقراءة العامة للمحتوى المنشور عند تنفيذ Story 5.7.

---

**تم الإنشاء بواسطة:** GPT-5 Codex (Cursor)  
**آخر تحديث:** 2025-11-08  

