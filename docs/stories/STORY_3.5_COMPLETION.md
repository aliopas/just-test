# Story 3.5: واجهة تقديم طلب جديد – حالة الإكمال

**التاريخ:** 2025-11-08  
**الحالة:** ✅ مكتمل

---

## ✅ ما تم إنجازه

### 1. صفحة الواجهة `/app/new-request`
- إنشاء نقطة دخول جديدة في `frontend/src/app/new-request/main.tsx` تشغّل الصفحة داخل React Strict Mode.
- بناء `NewRequestPage` مع تبني `LanguageProvider`, `ToastProvider`, و`QueryClientProvider` لتوحيد i18n والتنبيهات وإدارة الحالة.
- إضافة قسم تمهيدي (Before you submit) يوضح للمستثمر انتقال الطلب مباشرة إلى حالة “Submitted”.

### 2. نموذج الطلب الجديد
- `NewRequestForm` يستخدم `react-hook-form` + `zodResolver` مع المخطط `newRequestFormSchema` للتحقق من القيم (نوع الطلب، المبلغ، العملة، السعر المستهدف، تاريخ الصلاحية، الملاحظات).
- الرسومات والتخطيط باستخدام CSS Grid/Flex بما يتماشى مع نظام التصميم.
- زر حفظ يعرض حالة التحميل ورسالة نجاح/فشل عبر `ToastStack`.
- زر إعادة تعيين يعيد تعيين الحقول عند الحاجة.

### 3. رفع المرفقات (Placeholder)
- مكوّن `UploadDropzone` يوفر تجربة سحب وإفلات مع عرض قائمة الملفات (لا يتم الرفع فعليًا بعد، متوافق مع Story 3.4 مستقبلاً).
- التخزين المحلي للملفات عبر `useState` وعرض الحجم (MB).

### 4. الربط مع الـ API
- Hook `useCreateRequest` يستخدم `@tanstack/react-query` لإرسال طلب POST إلى `/investor/requests`.
- بعد النجاح يُرصد الحدث في `analytics` placeholder ويظهر Toast نجاح.

### 5. التدويل (i18n)
- `frontend/src/locales/newRequest.ts` يحتوي على الرسائل العربية/الإنجليزية لجميع عناصر النموذج.
- إعادة استخدام `LanguageProvider` لضبط الاتجاه (RTL/ LTR) وتغيير اللغة لاحقًا عند توسيع الواجهة.

---

## 🧪 الاختبارات
- لا توجد اختبارات UI تلقائية بعد. تم اختبار التدفق يدويًا عبر المتصفح المحلي (بعد إعداد bundler).
- التبعيات المطلوبة للواجهة (إن لم تكن مثبّتة):
  ```
  npm install react react-dom @types/react @types/react-dom
  npm install @tanstack/react-query react-hook-form @hookform/resolvers zod
  ```
- يوصى بإضافة اختبارات Playwright أو Testing Library في Stories لاحقة.

---

## 📁 الملفات المنشأة / المحدثة
- `frontend/src/types/request.ts`
- `frontend/src/schemas/newRequestSchema.ts`
- `frontend/src/hooks/useCreateRequest.ts`
- `frontend/src/locales/newRequest.ts`
- `frontend/src/components/request/NewRequestForm.tsx`
- `frontend/src/components/request/UploadDropzone.tsx`
- `frontend/src/pages/NewRequestPage.tsx`
- `frontend/src/app/new-request/main.tsx`
- تحديث `docs/front-end-spec.md` (الإصدار 1.2) و`README.md`

---

## 📌 ملاحظات
- التكامل الفعلي لرفع الملفات سيُنفَّذ في Story 3.4 (`/investor/requests/:id/files/presign`).
- يمكن ربط الصفحة مع التبديل اللغوي العام لاحقًا عبر مكوّن رئيسي أو Router.
- عند بناء الواجهة النهائية، يفضل إضافة ربط للصفحة من لوحة المستثمر (navigation).

---

**تم الإنشاء بواسطة:** GPT-5 Codex (Cursor)  
**آخر تحديث:** 2025-11-08  

