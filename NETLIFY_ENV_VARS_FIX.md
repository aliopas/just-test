# حل مشكلة متغيرات البيئة في Netlify

## المشكلة

الخطأ: `Missing Supabase environment variables: SUPABASE_URL and SUPABASE_ANON_KEY are required`

حتى لو كانت المتغيرات موجودة في Netlify Dashboard، قد تظهر هذه المشكلة بسبب:

1. **Scope غير صحيح** - المتغيرات موجودة لكن Scope ليس Production
2. **لم يتم إعادة النشر** - بعد إضافة المتغيرات، يجب إعادة النشر
3. **القيم غير صحيحة** - القيم موجودة لكنها خاطئة

---

## الحل خطوة بخطوة

### الخطوة 1: التحقق من Scope (النطاق)

1. **اذهب إلى Netlify Dashboard:**
   - افتح: https://app.netlify.com/sites/heartfelt-moxie-dfcdb4/configuration/env

2. **تحقق من كل متغير:**
   - اضغط على السهم ⬇️ بجانب `SUPABASE_URL`
   - اضغط على السهم ⬇️ بجانب `SUPABASE_ANON_KEY`

3. **تحقق من Scope:**
   - يجب أن ترى: **"Production"** مفعّل ✓
   - إذا لم يكن مفعّلاً:
     - اضغط على **"Edit"** (تعديل)
     - فعّل **"Production"** في قسم Scope
     - احفظ التغييرات

### الخطوة 2: التحقق من القيم

**القيم الصحيحة لمشروعك:**

- **SUPABASE_URL:**
  ```
  https://wtvvzthfpusnqztltkkv.supabase.co
  ```

- **SUPABASE_ANON_KEY:**
  ```
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Ind0dnZ6dGhmcHVzbnF6dGx0a2t2Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjIyMzE2MDUsImV4cCI6MjA3NzgwNzYwNX0.6KttJmjGUsgp3xfGf3wBm6kPmrinXB5R6AJJsTB-LWA
  ```

**إذا كانت القيم خاطئة:**
- اضغط على **"Edit"** (تعديل)
- صحح القيمة
- احفظ التغييرات

### الخطوة 3: إعادة النشر (مهم جداً!)

**⚠️ بعد أي تعديل على المتغيرات، يجب إعادة النشر!**

#### الطريقة الأولى: من Netlify Dashboard

1. **اذهب إلى صفحة Deploys:**
   - من القائمة الجانبية، اضغط على **"Deploys"**
   - أو افتح: https://app.netlify.com/sites/heartfelt-moxie-dfcdb4/deploys

2. **اضغط على "Trigger deploy":**
   - في الأعلى، اضغط على **"Trigger deploy"**
   - اختر **"Deploy site"** (نشر الموقع)
   - أو **"Clear cache and deploy site"** (مسح الكاش وإعادة النشر) - هذا أفضل

3. **انتظر حتى يكتمل النشر:**
   - قد يستغرق بضع دقائق
   - تحقق من أن الحالة أصبحت **"Published"** (تم النشر)

#### الطريقة الثانية: عبر GitHub

1. **اذهب إلى GitHub:**
   - افتح repository المشروع
   - قم بعمل أي تغيير بسيط (مثل تعديل README)
   - أو أنشئ commit فارغ:
     ```bash
     git commit --allow-empty -m "Trigger redeploy for env vars"
     git push
     ```

2. **Netlify سيكتشف التغيير تلقائياً:**
   - سيبدأ النشر التلقائي
   - انتظر حتى يكتمل

### الخطوة 4: التحقق من النجاح

بعد إعادة النشر:

1. **جرب الوصول إلى API:**
   - افتح: https://heartfelt-moxie-dfcdb4.netlify.app/api/v1/health
   - يجب أن يعمل بدون أخطاء
   - يجب ألا ترى خطأ "Missing Supabase environment variables"

2. **راجع Function Logs:**
   - اذهب إلى **"Functions"** → **"server"**
   - راجع السجلات (Logs) للتحقق من عدم وجود أخطاء

---

## إذا استمرت المشكلة

### الحل 1: حذف وإعادة إضافة المتغيرات

1. **احذف المتغيرات:**
   - في صفحة Environment Variables
   - اضغط على أيقونة الحذف 🗑️ بجانب `SUPABASE_URL`
   - اضغط على أيقونة الحذف 🗑️ بجانب `SUPABASE_ANON_KEY`

2. **أعد إضافتها:**
   - اضغط على **"Add a variable"**
   - أضف `SUPABASE_URL` مع القيمة الصحيحة
   - **Scope:** اختر **Production** ✓
   - احفظ
   - أضف `SUPABASE_ANON_KEY` مع القيمة الصحيحة
   - **Scope:** اختر **Production** ✓
   - احفظ

3. **أعد النشر:**
   - اذهب إلى Deploys
   - اضغط على "Trigger deploy" → "Clear cache and deploy site"

### الحل 2: التحقق من Build Logs

1. **اذهب إلى Deploys:**
   - اختر آخر نشر
   - اضغط على **"Build logs"**

2. **ابحث عن:**
   - أي أخطاء متعلقة بالمتغيرات
   - أي رسائل تحذيرية

### الحل 3: التحقق من Function Logs

1. **اذهب إلى Functions:**
   - من القائمة الجانبية، اضغط على **"Functions"**
   - اضغط على **"server"**

2. **راجع Logs:**
   - ابحث عن أي أخطاء
   - تحقق من أن المتغيرات موجودة في وقت التشغيل

---

## ملاحظات مهمة

1. **Scope مهم جداً:**
   - يجب أن تكون المتغيرات في **Production** scope
   - Deploy previews و Branch deploys قد لا تحتاجها، لكن Production ضروري

2. **إعادة النشر ضرورية:**
   - بعد أي تعديل على المتغيرات، يجب إعادة النشر
   - Netlify Functions تحصل على المتغيرات عند النشر، وليس عند التعديل

3. **القيم يجب أن تكون صحيحة:**
   - لا تضع مسافات قبل أو بعد القيم
   - انسخ القيم بالكامل بدون تعديل

4. **Cache:**
   - استخدم **"Clear cache and deploy site"** لإزالة الكاش
   - هذا يضمن أن التغييرات تُطبق بشكل صحيح

---

## روابط مفيدة

- **Environment Variables:** https://app.netlify.com/sites/heartfelt-moxie-dfcdb4/configuration/env
- **Deploys:** https://app.netlify.com/sites/heartfelt-moxie-dfcdb4/deploys
- **Functions:** https://app.netlify.com/sites/heartfelt-moxie-dfcdb4/functions

---

## الدعم

إذا استمرت المشكلة بعد تجربة جميع الحلول:
1. راجع Build Logs في Netlify
2. راجع Function Logs في Netlify
3. تحقق من أن المتغيرات موجودة في Production scope
4. تأكد من إعادة النشر بعد أي تعديل

