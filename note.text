خطة تحسين منصّة المستثمر–الأدمن (Bakurah Investors Portal)

خليني أكون واضح: الفكرة الأساسية جيدة، لكنك تحتاج بنية مُحكمة، دورة حياة للطلبات، وضبط أمني وتنفيذي من اليوم الأول. أدناه تحسين عملي مختصر وقابل للتنفيذ.

1) النطاق والوظائف الأساسية (MVP)

واجهة عامّة: صفحة هبوط فقط (خبر/إعلان مختصر + زر “تسجيل مستثمر”). لا محتوى حساس لغير المسجلين.

بوابة المستثمر:

تسجيل/تفعيل (OTP عبر SMS/Email) + 2FA اختياري.

ملف شخصي (بيانات الهوية/التواصل، تفضيلات التواصل، لغة: عربي/إنجليزي).

تقديم طلب: (شراء/بيع) مع الحقول + رفع مرفقات (إيصالات/حوالات).

متابعة الطلبات بحالات واضحة (انظر دورة الحياة).

إشعارات (Email + SMS + داخل المنصّة).

لوحة الأدمن:

إدارة الأخبار/الإعلانات (نشر/جدولة/إخفاء).

صندوق وارد للطلبات مع فلترة وفرز وحالة.

قبول/رفض/طلب معلومات إضافية + تعليقات داخلية.

إدارة المستخدمين (تعليق/تنشيط/إعادة تعيين كلمة مرور).

سجل تدقيق (監査) لكل إجراء.

2) دورة حياة الطلب (State Machine)

Draft (قبل الإرسال) → Submitted

Screening (مراجعة أولية/تحقق مستندات)

Pending Info (طلب معلومات إضافية من المستثمر)

Compliance Review (اختياري: KYC/AML إن لزم)

Approved أو Rejected

Settling (إثبات الدفع/المقاصة) → Completed
التحولات تُسجّل مع الوقت، المُنفّذ، والملاحظة.

3) النماذج والحقول المهمة
نموذج تسجيل المستثمر

الاسم الكامل/الكيان القانوني، رقم الهوية/السجل التجاري (اختياري حسب النوع)، الدولة/المدينة.

الجوال (مع Country Code)، البريد، لغة الواجهة.

تفعيل عبر OTP + قبول سياسة الخصوصية/الشروط.

نموذج طلب (شراء/بيع)

نوع الطلب: Buy | Sell.

المبلغ/الكمية/العملة، السعر المستهدف (إن وجد)، صلاحية العرض (تاريخ انتهاء).

وسيلة الدفع (تحويل/مدى/Apple Pay)، الملفات: إيصال/تحويل (PDF/JPG/PNG، حتى 10MB لكل ملف).

ملاحظات المستثمر.
تحقق: قيود أرقام موجبة، عملات مدعومة، تاريخ صلاحية ≥ اليوم، ملفات نظيفة (MIME/فيروس).

4) إدارة المحتوى (الأخبار/الإعلانات)

إنشاء/تحرير/جدولة نشر، تصنيف (صفقات، نتائج، توسعات).

دعم Markdown وصور (تحجيم تلقائي + ضغط).

صلاحيات: Publisher يطلب اعتماد، Approver ينشر.

5) الصلاحيات والأمن

أدوار: Investor, Admin, Publisher, Approver, Auditor (قراءة فقط للسجلات).

جلسات آمنة: JWT قصير + Refresh، CSRF للواجهات، Rate Limiting.

2FA (TOTP/SMS)، كلمات مرور مُملّحة (Argon2/Bcrypt).

RBAC دقيق على مستوى الإجراءات والسجلات.

تنظيف البيانات الحساسة في السجلات (لا تسجّل أرقام كاملة أو مفاتيح).

CSP, XSS/SQLi hardening، فحص مرفقات/AV إن أمكن.

6) الملفات والتخزين

رفع إلى Object Storage (S3-compatible) مع:

مسارات مفصولة لكل طلب.

Presigned URLs للرفع/التنزيل.

تشفير at-rest (SSE) وin-transit (TLS).

سياسة احتفاظ (Retention) + حظر التعديل بعد الإقفال (WORM) إن لزم.

7) الإشعارات والتواصل

قوالب Email/SMS جاهزة للحالات: Submitted, Pending Info, Approved, Rejected, Settling, Completed.

مركز إشعارات داخل المنصّة + تفضيلات (Mute/Channels).

سجل مراسلات مرتبط بكل طلب (Timeline).

8) التقارير ولوحات المتابعة

لوحة المستثمر: ملخص محافظ/طلبات، حالة كل طلب، إجراءات مطلوبة.

لوحة الأدمن:

عدد الطلبات/الحالات، متوسط زمن المعالجة، طلبات متعثرة (> X أيام).

نشاط المحتوى (مشاهدات/تفاعلات).

تصدير CSV مع ترقيم طلبات فريد (INV-YYYY-####).

9) واجهات برمجية (API) – مثال مختصر

POST /auth/register, POST /auth/verify-otp, POST /auth/login, POST /auth/refresh

GET /news?status=published (عام يقرأ العناوين المختصرة)، GET /news/:id

POST /investor/requests (إنشاء)، GET /investor/requests, GET /investor/requests/:id

POST /investor/requests/:id/files/presign (رفع مرفقات)

PATCH /admin/requests/:id (تغيير حالة/تعليق)

POST /admin/news PATCH /admin/news/:id POST /admin/news/:id/publish

كل نقطة بنطاق وصلاحية واضحة + pagination وfiltering.

10) قاعدة البيانات (تصميم موجز)

users: id, name, email, phone_cc, phone, role, status, mfa_enabled, created_at

investor_profiles: user_id, id_type, id_number, nationality, kyc_status

requests: id, user_id, type(buy|sell), amount, currency, target_price, expiry_at, status, created_at, updated_at

request_events: request_id, from_status, to_status, actor_id, note, created_at

attachments: id, request_id, filename, mime, size, storage_key, created_at

news: id, title, slug, body_md, cover_key, category, status(draft|scheduled|published), scheduled_at, published_at, author_id

notifications: id, user_id, type, channel, payload, read_at, created_at

audit_logs: id, actor_id, action, target_type, target_id, diff, ip, ua, created_at

11) تجربة المستخدم (UX) – سريع

المستثمر:

صفحة الطلب من خطوة واحدة واضحة، تحذيرات فورية للتحقق.

مؤشرات حالة بصرية + شريط تقدم.

مرفقات بالسحب والإفلات + عرض مصغّر.

الأدمن:

Inbox شبيه بالبريد مع فلاتر (الحالة/المبلغ/التاريخ).

لوحة القرار (مراجعة بيانات + مرفقات + سجل حركي + تعليقات).

“طلب معلومات إضافية” بزر واحد يرسل قالب جاهز.

12) الامتثال والخصوصية (حسب الحاجة)

KYC/AML خفيف (تجميعي) الآن، تكامل مع مزود خارجي لاحقًا.

سياسة خصوصية، شروط استخدام، بنود إخلاء مسؤولية مالية.

تتبع موافقة المعالجة (Consent) لكل مستخدم.

حذف/إخفاء بيانات عند الطلب (Right to Erasure) إن لزم.

13) التقنية والتنفيذ

Frontend: Next.js/React + i18n + SSR للحساسية والأداء.

Backend: NestJS/Express أو Laravel (حسب فريقك)، طبقة Service + Repository + DTOs.

DB: Postgres + Prisma/TypeORM.

Storage: S3 (Wasabi/MinIO/AWS).

Auth: JWT + Refresh + 2FA (Speakeasy/TOTP).

Queue/Jobs: BullMQ / Laravel Queues للإشعارات ومعالجة الملفات.

البنية: Docker + CI/CD (GitHub Actions) + مراقبة (Prometheus/Grafana) + Logging مركزي (ELK/OPENSEARCH).

الحماية: WAF/Cloudflare، أسرار عبر Vault/SSM.

14) المقاييس (KPIs)

TAT معالجة الطلب (متوسط/90th percentile).

نسبة الطلبات Pending Info > X أيام.

معدل إتمام الرفع بنجاح، معدل فشل الإشعارات.

تفاعل الأخبار (CTR/Views).

15) خارطة الطريق

MVP (4–6 أسابيع): تسجيل/تسجيل دخول + تقديم طلب + حالات أساسية + أخبار منشورة + رفع ملفات + إشعارات Email + RBAC بسيط + تقارير أولية.

المرحلة 2: 2FA، SMS، جدولة الأخبار، KYC تكاملي، تدقيق مرفقات/AV، تصدير تقارير، أرشفة WORM.

المرحلة 3: تطبيق موبايل، تكامل دفع مباشر، Dashboard تحليلي متقدم، مهام آلية SLA.

16) معايير قبول (نماذج)

عند تقديم طلب شراء بمبلغ موجب وعملة مدعومة مع مرفق صحيح → الحالة تتحول إلى Submitted ويصل إشعار للمستثمر.

عند “طلب معلومات إضافية” من الأدمن → الحالة Pending Info + إشعار + يفتح حقل رد للمستثمر.

لا يستطيع مستخدم غير مسجل مشاهدة الأخبار الداخلية أو بيانات الطلبات.

رفع ملف >10MB أو نوع غير مسموح يُرفض فورًا برسالة عربية واضحة.

🧩 دورة نشر الأخبار والإعلانات:

إنشاء الخبر أو الإعلان داخل لوحة التحكم (Admin Panel).

يضيف الأدمن: العنوان، المحتوى، الصورة، الفئة (صفقات – توسعات – أخبار عامة – إعلانات للمستثمرين).

يمكنه حفظها كـ مسودة (Draft) أو جدولة النشر (Scheduled) أو نشر فوري (Published).

المراجعة والموافقة (اختياري)

في النظام المتقدم يمكن أن يكون هناك مستخدم آخر بصلاحية “Approver” يراجع المحتوى قبل النشر الرسمي.

النشر (Publish)

عند النشر، تنتقل الحالة إلى "Published" وتظهر تلقائيًا في واجهة المستثمرين (Dashboard أو صفحة "الأخبار").

كل خبر يحتوي على: العنوان، التاريخ، صورة مصغّرة، وملخّص.

عند النقر عليه، تظهر صفحة التفاصيل الكاملة.

الإشعارات

يتم إرسال إشعار داخل المنصة أو عبر البريد الإلكتروني للمستثمرين المشتركين بالتحديثات.

🔒 العرض المسموح:

غير المسجّلين لا يمكنهم رؤية أي محتوى سوى صفحة التسجيل أو صفحة ترحيبية مختصرة.

المستثمرين المسجلين فقط يشاهدون الأخبار والتحديثات الكاملة بعد تسجيل الدخول.

كذلك 🧩 أولًا: إنشاء المستثمر من لوحة الأدمن

يستعملها الأدمن في حال:

يرغب بإضافة مستثمر جديد يدويًا (بناءً على توقيع عقد خارجي أو شراكة جديدة).

المستثمر لا يملك حساب مسبقًا في المنصة.

⚙️ خطواتها داخل لوحة التحكم:

إنشاء حساب جديد

يضغط الأدمن على “إضافة مستثمر جديد”.

يدخل البيانات الأساسية:

الاسم الكامل / اسم الكيان التجاري

رقم الجوال + مفتاح الدولة

البريد الإلكتروني

الدولة / المدينة

نوع الحساب (فردي / شركة)

الحالة (نشط / موقوف / بانتظار التفعيل)

يحدد كلمة مرور مؤقتة أو يرسل رابط تفعيل للحساب عبر البريد أو SMS.

إرفاق الوثائق (اختياري)

مثل السجل التجاري، الهوية، أو اتفاقية الاستثمار الموقعة.

تفعيل الحساب

يمكن للأدمن تفعيله مباشرة أو ينتظر تأكيد المستثمر عبر الرابط المرسل.

صلاحيات المستخدم

تعيين الدور “Investor” (لا يمكنه دخول لوحة الأدمن).

في الأنظمة المتقدمة، يمكن تحديد نوع المستثمر (مستثمر رئيسي – مستثمر جزئي – مراقب فقط).

 الغات المقترحه 
 css
 node.js
 javascript 
 react
 