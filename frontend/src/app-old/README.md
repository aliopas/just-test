# ⚠️ هذا المجلد قديم وغير مستخدم

هذا المجلد (`src/app/`) هو نسخة قديمة من App Router.

**المجلد المستخدم حالياً:** `frontend/app/`

يرجى استخدام `app/` بدلاً من `src/app/`.

في Next.js 16، يجب أن يكون هناك مجلد `app` واحد فقط في جذر المشروع.

---

# ⚠️ This directory is deprecated and not used

This directory (`src/app/`) is an old version of the App Router.

**Current directory in use:** `frontend/app/`

Please use `app/` instead of `src/app/`.

In Next.js 16, there should be only one `app` directory at the project root.

