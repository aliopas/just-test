# دليل اختبار المنصة باستخدام MCP Tools

## 🚀 خطوات بدء الخادم والاختبار

### 1. بدء خادم التطوير

**الطريقة 1: استخدام PowerShell Script**
```powershell
cd frontend
.\start-dev-server.ps1
```

**الطريقة 2: يدوياً**
```powershell
cd frontend
npm run dev
```

الخادم سيعمل على: `http://localhost:3002`

### 2. التحقق من حالة الخادم باستخدام MCP

بعد بدء الخادم، استخدم أدوات MCP التالية:

#### أ) اكتشاف الخادم
```javascript
// استخدم nextjs_index للتحقق من الخوادم النشطة
// يجب أن يظهر خادم على المنفذ 3002
```

#### ب) فحص الأخطاء في الوقت الفعلي
```javascript
// استخدم nextjs_call مع:
// port: "3002"
// toolName: "get_errors" أو "get_routes"
```

### 3. اختبار الصفحات باستخدام Browser Automation

بعد بدء الخادم، يمكن استخدام `browser_eval` لاختبار:

- ✅ `/` - الصفحة الرئيسية
- ✅ `/login` - صفحة تسجيل الدخول  
- ✅ `/home` - لوحة المستثمر
- ✅ `/admin/dashboard` - لوحة الإدارة
- ✅ `/register` - صفحة التسجيل
- ✅ `/verify` - صفحة التحقق

## 📋 قائمة الأدوات المتاحة في MCP

### Next.js MCP Tools (Next.js 16+)

1. **nextjs_index** - اكتشاف الخوادم النشطة
2. **nextjs_call** - استدعاء أدوات Next.js:
   - `get_errors` - الحصول على الأخطاء
   - `get_routes` - الحصول على قائمة المسارات
   - `get_build_status` - حالة البناء
   - `clear_cache` - مسح الكاش

### Browser Automation Tools

1. **browser_eval** - اختبار الصفحات:
   - `start` - بدء المتصفح
   - `navigate` - الانتقال إلى URL
   - `screenshot` - التقاط صورة
   - `console_messages` - رسائل Console
   - `evaluate` - تنفيذ JavaScript

## 🔍 خطوات الاختبار الشاملة

### الخطوة 1: بدء الخادم
```bash
cd frontend
npm run dev
```

### الخطوة 2: انتظر حتى يبدأ الخادم
سترى رسالة مثل:
```
✓ Ready in X seconds
○ Local: http://localhost:3002
```

### الخطوة 3: استخدام MCP للتحقق
1. استخدم `nextjs_index` للتحقق من الخادم
2. استخدم `nextjs_call` مع `get_errors` لفحص الأخطاء
3. استخدم `nextjs_call` مع `get_routes` لرؤية جميع المسارات

### الخطوة 4: اختبار الصفحات
استخدم `browser_eval`:
- `navigate` إلى `http://localhost:3002`
- `console_messages` للتحقق من الأخطاء
- `screenshot` لرؤية الصفحة

## ⚠️ ملاحظات مهمة

1. **MCP متاح تلقائياً في Next.js 16+**
   - لا يحتاج إعدادات إضافية
   - يعمل على `/_next/mcp` تلقائياً

2. **إذا لم يظهر الخادم في nextjs_index:**
   - تأكد من أن Next.js 16+ مثبت
   - تأكد من أن الخادم يعمل على المنفذ 3002
   - انتظر بضع ثوانٍ بعد بدء الخادم

3. **للتحقق من الأخطاء:**
   - استخدم `nextjs_call` مع `get_errors`
   - استخدم `browser_eval` مع `console_messages`

## 📊 أمثلة على الاستخدام

### مثال 1: فحص الأخطاء
```javascript
// بعد بدء الخادم
nextjs_index() // للعثور على الخادم
nextjs_call({
  port: "3002",
  toolName: "get_errors"
}) // للحصول على الأخطاء
```

### مثال 2: اختبار الصفحة الرئيسية
```javascript
browser_eval({
  action: "start"
})
browser_eval({
  action: "navigate",
  url: "http://localhost:3002"
})
browser_eval({
  action: "console_messages",
  errorsOnly: true
})
```

## ✅ التحقق من النجاح

المنصة تعمل بشكل صحيح إذا:
- ✅ الخادم يبدأ بدون أخطاء
- ✅ `nextjs_index` يكتشف الخادم
- ✅ `get_errors` لا يعيد أخطاء
- ✅ الصفحات تتحمّل في المتصفح
- ✅ لا توجد أخطاء في Console

---

**جاهز للاختبار!** 🚀

