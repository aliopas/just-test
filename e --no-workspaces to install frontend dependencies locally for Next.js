[1mdiff --git a/netlify.toml b/netlify.toml[m
[1mindex 5b2cd10..3689838 100644[m
[1m--- a/netlify.toml[m
[1m+++ b/netlify.toml[m
[36m@@ -5,8 +5,8 @@[m
   # Sequential execution ensures proper dependency installation[m
   # Simplified command to reduce build time and prevent cancellation[m
   # Since we use npm workspaces, install from root first, then ensure frontend dependencies are available[m
[31m-  # Next.js needs react/react-dom in frontend/node_modules, so we install them there too[m
[31m-  command = "echo 'Starting build...' && cd .. && echo 'Installing root dependencies (workspace)...' && npm install --legacy-peer-deps && cd frontend && echo 'Ensuring frontend dependencies are installed...' && npm install --legacy-peer-deps && cd ../netlify/functions && echo 'Installing function dependencies...' && npm install --production --no-optional --legacy-peer-deps && cd ../../frontend && echo 'Building frontend...' && npm run build"[m
[32m+[m[32m  # Next.js needs react/react-dom in frontend/node_modules, so we install them locally with --no-workspaces[m
[32m+[m[32m  command = "echo 'Starting build...' && cd .. && echo 'Installing root dependencies (workspace)...' && npm install --legacy-peer-deps && cd frontend && echo 'Installing frontend dependencies locally (required for Next.js)...' && npm install --legacy-peer-deps --no-workspaces && cd ../netlify/functions && echo 'Installing function dependencies...' && npm install --production --no-optional --legacy-peer-deps && cd ../../frontend && echo 'Building frontend...' && npm run build"[m
   functions = "../netlify/functions"[m
   publish = ".next"[m
   base = "frontend"[m
